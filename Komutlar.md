#Python'da kullanılabilecek terminal komutlarını kopyalamanız için bir liste      #
```python
python           # Python yorumlayıcısını başlatır.
pip              # Python paketlerini yönetir (kurma, kaldırma, güncelleme vb.).
python dosya_adı.py   # Python betik dosyasını çalıştırır.
virtualenv       # Sanal Python ortamları oluşturur.
activate (Windows) / source activate (Unix)   # Sanal ortamı etkinleştirir.
deactivate       # Sanal ortamı devre dışı bırakır.
pip freeze       # Kurulu Python paketlerini ve sürümlerini listeler.
pip install paket_adı   # Belirli bir Python paketini kurar.
pip uninstall paket_adı   # Belirli bir Python paketini kaldırır.
pip show paket_adı   # Belirli bir Python paketinin bilgilerini gösterir.
pip search anahtar_kelime   # Anahtar kelimeye göre Python paketlerini arar.
pip list         # Kurulu Python paketlerini listeler.
python -m venv myenv   # Yeni bir sanal ortam oluşturur (Python 3.3+).
python -V (veya --version)   # Yüklü Python sürümünü görüntüler.
python -m pip install --upgrade pip   # Pip'in en son sürümünü yükseltir.
python -m http.server [port]   # Basit bir HTTP sunucusu başlatır.
python -c "print('Hello, world!')"   # Bir Python ifadesini doğrudan terminalden çalıştırır.
exit() veya Ctrl+D   # Python yorumlayıcısını kapatır.
```

#  Windows terminalinde kullanılan yaygın komutların bir listesi      #

```python
assoc      # Dosya uzantılarını programlarla ilişkilendirir.
attrib      # Dosya ve dizinlerin özniteliklerini değiştirir.
bcdedit      #  Windows başlangıç yapılandırmasını düzenler.
break      # For döngüsünü veya bir batch dosyasını durdurur.
cacls      # Dosya ve dizinlerin erişim haklarını değiştirir.
call      # Bir batch dosyasındaki başka bir komut dosyasını çağırır.
certreq      # Sertifika isteği oluşturur ve sertifika yetkililerine gönderir.
chkntfs      # Dosya sistemi hatalarını kontrol eder ve düzeltir.
cls      # Komut istemini temizler.
color      # Komut istemi için renkleri değiştirir.
compact      # Dosyaları veya dizinleri sıkıştırır veya açar.
convert      # Disk biçimini değiştirir.
date      # Sistem tarihini görüntüler veya ayarlar.
debug      # Dosya veya bellek içeriğini düzenler.
dism      # Windows görüntülerini yönetir.
diskcomp      # İki disket veya dosya arasındaki farkları karşılaştırır.
diskcopy      # Disketleri veya disk imaj dosyalarını kopyalar.
djoin      # Bir bilgisayarı bir etki alanına katılır veya kaldırır.
doskey      # Komut istemi girişini kaydeder ve yeniden kullanılabilir.
driverquery      # Yüklü aygıt sürücülerini listeler.
echo      # Bir metni ekrana yazdırır.
endlocal      # Yerel değişkenleri sıfırlar veya yeniden yükler.
erase      # Dosyaları siler.
fc      # İki dosya veya dosya setini karşılaştırır ve farkları görüntüler.
find      # Metin içinde belirli bir deseni arar.
for      # Bir döngüde belirtilen bir dizi komutu çalıştırır.
format      # Diski biçimlendirir.
fsutil      # Dosya sistemi özelliklerini yönetir.
ftype      # Dosya türlerini belirli bir uygulamaya ilişkilendirir.
goto      # Bir batch dosyasındaki belirli bir etikete atlar.
gpresult      # Grup ilkesi sonuçlarını görüntüler.
graftabl      # Karakter seti tablosunu görüntüler veya değiştirir.
hostname      # Bilgisayarın adını görüntüler veya ayarlar.
label      # Disk etiketini veya birim adını değiştirir.
mode      # Sistem aygıtlarını yapılandırır.
more      # Metin içeriğini sayfa sayfa görüntüler.
net      # Ağ kaynaklarını yönetir.
openfiles      # Açık dosyaları ve bağlantıları listeler veya kapatır.
path      # Komut istemi için arama yolu belirler veya görüntüler.
pause      # Bir batch dosyasının çalışmasını duraklatır ve bir tuşa basılmasını bekler.
print      # Dosyaları yazıcıya gönderir.
pushd      # Bir dizini yığın hafızasına ekler ve sonra başvurmak için kullanılabilir.
popd      # Yığındaki en son dizine geri döner.
query      # Sistem bilgilerini veya ayarlarını sorgular.
reg      # Kayıt defteri işlemlerini gerçekleştirir.
recover      # Hatalı veya silinen dosyaları kurtarır.
rem      # Bir batch dosyasında açıklama ekler.
ren      # Dosya adını değiştirir.
replace      # Dosyaları birbirleriyle değiştirir.
rmdir      # Dizinleri siler.
set      # Ortam değişkenlerini ayarlar veya görüntüler.
setlocal      # Geçici bir çalışma alanı içinde değişkenleri tanımlar.
shutdown      # Bilgisayarı kapatır veya yeniden başlatır.
sort      # Satırları sıralar.
start      # Programları veya dosyaları başlatır.
subst      # Bir yolun bir sürücü harfiyle ilişkilendirilmesini sağlar.
systeminfo      # Sistem bilgilerini görüntüler.
time      # Sistem saati ve tarihini ayarlar veya görüntüler.
title      # Komut istemi penceresinin başlığını ayarlar.
tree      # Dizin yapısını ağaç şeklinde görüntüler.
type      # Dosya içeriğini görüntüler.
ver      # Windows sürümünü görüntüler.
verify      # Dosyaların doğru bir şekilde yazıldığını doğrular.
vol      # Disk hacmi etiketini görüntüler.
xcopy      # Dosyaları veya dizinleri kopyalar ve dizin yapısını korur.
wmic      # Windows Yönetim Araçları Komut Satırı Arabirimini başlatır.
icacls      # Dosya ve dizinlerin ACL lerini yönetir.

```